---
interface Props {
  text: string;
  icon?: string;
}

const { text, icon } = Astro.props;
---

<button class="bg-orange-500 text-white h-[55px] rounded-md w-auto p-auto"
  >{text} {icon}</button
>
